.venv/
__pycache__/
*.pyc
*.egg-info/
.vs/
.cora/
.claude/
secrets.json
# config.json is deliberately NOT ignored here, unlike in the sibling projects
# (CONVENTIONS.md 4.2). It carries no credentials, only the definition of the
# reasoning measurement instrument, which must be identical for anyone who
# clones the repo. See README.md > Configuration.
CLAUDE.md
Projects/